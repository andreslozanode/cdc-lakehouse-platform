# Notebooks

Espacio para EDA y prototipado. Conexiones recomendadas:
- **ClickHouse** (`clickhouse-connect`) para features online y agregados gold.
- **Iceberg/PyIceberg** para datasets de entrenamiento point-in-time (time-travel).

No subir notebooks con outputs/credenciales; usar `jupyter nbconvert --clear-output`.
